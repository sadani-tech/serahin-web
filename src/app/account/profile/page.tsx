import { api } from "@/lib/api";
import { ProfileForms } from "./ProfileForms";
export default async function ProfilePage({ searchParams }: { searchParams: Promise<{ callbackUrl?: string }> }) { const profile = await api.get<{ name: string; email: string | null; phone: string | null; hasPassword: boolean; avatarUrl: string | null }>("/auth/buyer/profile"); const { callbackUrl } = await searchParams; return <main className="mx-auto max-w-5xl px-4 py-10 sm:px-6"><h1 className="mb-6 text-3xl font-extrabold text-sand-900">Profil Buyer</h1><ProfileForms profile={profile} callbackUrl={callbackUrl}/></main>; }
